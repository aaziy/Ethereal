# Etherial Environment Configuration
# ════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────
# Node Environment
# ────────────────────────────────────────────────────────────────
NODE_ENV=development

# ────────────────────────────────────────────────────────────────
# Server Configuration
# ────────────────────────────────────────────────────────────────
# Port for the development server
PORT=3000

# Gun.js relay server port
GUN_RELAY_PORT=8765

# ────────────────────────────────────────────────────────────────
# Gun.js Relay Configuration
# ────────────────────────────────────────────────────────────────
# Public URL of Gun.js relay (accessible from browser)
# For development: http://localhost:8765/gun
# For production: https://your-domain.com/gun
NEXT_PUBLIC_GUN_RELAY=http://localhost:8765/gun

# Optional: Remote relay servers to sync with
# Separate multiple relays with commas
GUN_RELAY_PEERS=

# ────────────────────────────────────────────────────────────────
# Database Configuration
# ────────────────────────────────────────────────────────────────
# Gun.js data storage path
GUN_DATA_DIR=.gun-data

# Enable debug logging for Gun.js
GUN_DEBUG=false

# ────────────────────────────────────────────────────────────────
# Application Settings
# ────────────────────────────────────────────────────────────────
# Rumor voting window duration (in hours)
VOTING_WINDOW_HOURS=48

# Minimum voters required for resolution
MIN_VOTERS=5

# Minimum total weight (√karma) required for resolution
MIN_TOTAL_WEIGHT=10

# Ratio threshold for FACT (≥ this ratio = FACT)
FACT_THRESHOLD=0.60

# Ratio threshold for FALSE (≤ this ratio = FALSE)
FALSE_THRESHOLD=0.40

# ────────────────────────────────────────────────────────────────
# Reputation/Karma Settings
# ────────────────────────────────────────────────────────────────
# Initial karma for new users
INITIAL_KARMA=1

# Karma gained for voting with majority
KARMA_CORRECT_VOTE=1.0

# Karma lost for voting against majority
KARMA_WRONG_VOTE=-1.5

# Karma lost for posting false rumor
KARMA_FALSE_POST=-2.0

# ────────────────────────────────────────────────────────────────
# Time Sync Configuration
# ────────────────────────────────────────────────────────────────
# Acceptable clock skew (in seconds)
CLOCK_SKEW_TOLERANCE=300

# ────────────────────────────────────────────────────────────────
# Feature Flags
# ────────────────────────────────────────────────────────────────
# Enable opposition/challenge mechanism
ENABLE_OPPOSITION=true

# Enable ghost deletion system
ENABLE_GHOST_SYSTEM=true

# Enable debug logging
DEBUG_MODE=false

# ────────────────────────────────────────────────────────────────
# Build & Performance
# ────────────────────────────────────────────────────────────────
# Next.js configuration
NEXT_TELEMETRY_DISABLED=1

# Enable source maps (set to false in production)
NEXT_PUBLIC_SOURCE_MAPS=true

# ════════════════════════════════════════════════════════════════
# NOTES FOR DEVELOPERS:
# ════════════════════════════════════════════════════════════════
#
# 1. Copy this file to .env.local for development:
#    cp .env.example .env.local
#
# 2. Email & Passphrase Authentication:
#    - Email is NEVER stored on any server
#    - Passphrase + Email → Deterministic keypair (Gun.SEA)
#    - Same credentials = same identity across sessions
#
# 3. Gun.js Relay Configuration:
#    - Default: http://localhost:8765/gun (local relay)
#    - Can sync with multiple remote relays
#    - All data is encrypted and cryptographically signed
#
# 4. Security:
#    - No passwords stored (authentication is blind)
#    - No central server (fully decentralized)
#    - P2P network handles all replication
#    - Gun.SEA provides cryptographic security
#
# 5. For Production Deployment:
#    - Change NODE_ENV to production
#    - Update NEXT_PUBLIC_GUN_RELAY to your domain
#    - Configure GUN_RELAY_PEERS for multi-node setup
#    - Enable production-grade relay server
#    - Disable DEBUG_MODE
#
# ════════════════════════════════════════════════════════════════
